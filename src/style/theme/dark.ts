import { DefaultTheme } from "styled-components";
import { ThemeProps } from "./styled";

const Dark: DefaultTheme = {
  title: 'dark',
  colors: {
    primary: '#000',
    secondary: '#fff'
  }
}

export default Dark;